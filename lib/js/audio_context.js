// const AudioContext = window.AudioContext || window.webkitAudioContext;
//
// const context = new AudioContext();
//
// context.destination;
//
// const snare = context.decodeAudioData(compressedBuffer).then(function(decodedData));
// const gain = context.createGain();
//
//
//
// snare.connect(gain);
// gain.connect(context.destination);
//
//
//
// snare.start(0);
//
// snare.stop(0);
//
// snare.disconnect(0);
